"projects": [
		<% @projects.each_with_index do |project, index|%>
		{
		  "index": <%= index %>, "id": <%= project.id.to_json %>, "name": "<%= project.name %>", "parent_id": <%= parent_id.to_json %>
		}
			<% if project != @projects.last %>
			,
			<% end %>
		<% end %>
	]